---
title: "Entropy & Probabilistic Optimization"
description: "Como o LATS-P utiliza entropia para medir incerteza e decidir quando intervir."
---

# Entropy & Probabilistic Optimization

A **entropia** é usada pelo LATS-P para detectar:

- incerteza  
- ambiguidade  
- conflito de interpretações  
- falta de contexto  
- respostas improváveis  

---

# Cálculo básico de entropia

O agente calcula a entropia da distribuição de probabilidades retornada pelo LLM:

```txt
H = − Σ (p_i * log(p_i))
```

Onde:
- H alto → alto grau de incerteza
- H baixo → decisão clara

O que o agente faz com essa informação?
🟣 Se a entropia (menor) threshold

→ prossegue normalmente.

🟠 Se a entropia ~ média

→ tenta recuperar mais contexto (RAG expansion).

🔴 Se a entropia (maior) threshold crítico

→ ativa HITL automaticamente.

---
## Otimização contínua

O agente armazena entropia por nó:
- média por caminho
- variação entre execuções
- correlações com erros reais
- padrões de confusão

Isso permite sugerir:

- melhorias na árvore
- novos nós
- reescrita de perguntas
- adição de dados
- aumento da granularidade